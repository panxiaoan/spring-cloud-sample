server:
   port: 9081
   
spring:
    application: 
        name: order-service

eureka:
   instance:
       hostname: localhost
   client:
       healthcheck: 
            enabled: true
       serviceUrl:
           defaultZone: http://localhost:4000/eureka/