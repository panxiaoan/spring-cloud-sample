server:
   port: 9080
   
spring:
    application: 
        name: member-service

eureka:
   instance:
       hostname: 192.168.246.146
       prefer-ip-address: true
   client:
       healthcheck: 
            enabled: true
       serviceUrl:
           defaultZone: http://${eureka.instance.hostname}:4000/eureka/