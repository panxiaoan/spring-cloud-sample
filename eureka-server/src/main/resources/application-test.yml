server:
  port: 4000
spring:
  application:
    name: eureka-server
eureka:
  instance:
    hostname: 192.168.246.146
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
management:
  security:
    enabled: false